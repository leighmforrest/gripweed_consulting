---
const { url, shortTitle, image } = Astro.props;
---
<div class="flex flex-col items-center justify-center border overflow-hidden border-gray-600 dark:border-gray-400 rounded-2xl cursor-pointer">
    <a href={url}>
        <img src={image.url} alt={image.alt} class="w-full aspect-square object-cover">
</a>
    <div class="p-4 text-center">
        <a class="text-2xl font-semibold hover:underline  text-gray-800 dark:text-gray-300" href={url}>{shortTitle}</a>
    </div>
</div>